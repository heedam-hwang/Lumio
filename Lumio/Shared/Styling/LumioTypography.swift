import SwiftUI

enum LumioTypography {
    static let floatingActionSymbol = Font.title3.bold()
    static let cardTitle = Font.headline.weight(.semibold)
    static let metadataAccent = Font.caption.weight(.semibold)
    static let compactAction = Font.caption.bold()
    static let helperText = Font.footnote
    static let bodySecondary = Font.subheadline
    static let menuLabel = Font.headline
}
